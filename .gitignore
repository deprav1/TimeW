server/data/
server/.env
# Адрес шлюза и токен устройства для сборки часов: генерируется
# `npm run setup` в server/, содержит секрет, в репозиторий не попадает.
watch/src/common/config.local.js
watch/node_modules/
watch/dist/
watch/build/
server/node_modules/
.temp_watch/
.research-open-vela-docs/
.claude/settings.local.json
deno.lock
# Токен доступа Deno Deploy: только локально, никогда в репозиторий
.deno-token
deno.txt
