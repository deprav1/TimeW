{
  "name": "timew-watch",
  "version": "0.2.0",
  "private": true,
  "engines": { "node": ">=8.10" },
  "scripts": {
    "pretest": "node scripts/prepare.mjs",
    "test": "node --import ./testkit/register.mjs --test",
    "prestart": "node scripts/prepare.mjs",
    "start": "aiot start --watch",
    "prebuild": "node scripts/prepare.mjs --bump",
    "build": "aiot build",
    "postbuild": "node scripts/show-rpk.mjs",
    "prerelease": "node scripts/prepare.mjs --bump",
    "release": "aiot release",
    "postrelease": "node scripts/show-rpk.mjs"
  },
  "devDependencies": {
    "@aiot-toolkit/jsc": "^1.0.3",
    "aiot-toolkit": "^2.0.5"
  }
}
