# Сценарии приложения и их проверка

Полный список того, что умеет TimeW, и чем каждый сценарий проверен. Колонка
«Проверка» отвечает на вопрос «откуда известно, что это работает».

Обозначения:

- **авто** — покрыто автотестом, гоняется командой `npm test`;
- **вручную** — проверено прогоном на этой машине;
- **на часах** — проверить можно только на живом устройстве,
  см. `physical-checklist.md`.

Итого автотестов: **213** (120 на шлюзе, 93 на логике часов).

## Подготовка и запуск

| Сценарий | Проверка |
| --- | --- |
| Первый запуск: `npm run setup` создаёт `.env`, генерирует токен, находит адрес в домашней сети, пишет `config.local.js` | вручную |
| Повторный `setup` не меняет токен и не ломает уже настроенное | вручную |
| `setup --new-token` перевыпускает токен | вручную |
| `setup --localhost --url …` настраивает вариант с туннелем | вручную |
| Токен не попадает в git (`config.local.js` в `.gitignore`) | вручную |
| `npm start` поднимает шлюз | вручную |
| `npm run autostart` ставит автозапуск, `--status` и `--off` управляют им | `--status` вручную; регистрация задачи меняет систему, выполняется владельцем |
| `npm run smoke` проверяет живой шлюз, токен берёт из `.env` | вручную: 20 базовых проверок; ещё одна с `--audio` |
| `npm run build` поднимает `versionCode` и печатает путь к `.rpk` | вручную |
| Сборка проходит, даже если `setup` не запускали (создаётся заглушка) | вручную |

## Шлюз

| Сценарий | Проверка |
| --- | --- |
| `GET /health` открыт без токена и показывает режим | авто |
| `GET /api/v1/status` требует токен, отдаёт возможности без секретов | авто |
| Запрос без токена отклоняется (401), сравнение токена устойчиво ко времени | авто |
| Вопрос уходит модели, ответ возвращается | авто |
| Заметка распознаётся по формулировке и сохраняется | авто |
| Список заметок, ограничение `?limit=N`, удаление по id | авто |
| Повтор запроса с тем же ключом не создаёт дубль; чужое тело с тем же ключом → 409 | авто |
| Точная команда света выполняется сразу, возвращает `undoToken`; повторная отмена/истёкший токен → 410 | авто |
| Неоднозначная комната и небезопасный класс устройств не исполняются | авто |
| Голос: распознавание и ответ одним запросом | авто |
| `intent=note&preview=1` возвращает черновик и ничего не сохраняет | авто |
| `intent=note` не даёт исполнить домашнюю команду | авто |
| Приём и сырых байтов, и `multipart/form-data` | авто |
| Озвучка: Gemini использует тот же ключ; отдельный OpenAI-совместимый TTS не получает ключ Gemini | авто |
| `speechId` живёт ограниченное время, затем 404 | авто |
| Зависший провайдер обрывается по таймауту → 504 | авто |
| Недоступный провайдер → 502, а не голый 500 | авто |
| Ошибка Tuya (устройство офлайн) → внятный 502 | авто |
| Ограничение частоты запросов → 429 | авто |
| Слишком большое тело → 413, пустое → 400, битый multipart → 400 | авто |
| Повреждённый `notes.json` изолируется, список продолжает работать | авто |
| Запись заметок атомарная, временный файл не остаётся | авто |
| Корректное завершение по сигналу | вручную (systemd/планировщик) |

## Часы: логика

| Сценарий | Проверка |
| --- | --- |
| На чистых часах адрес и токен берутся из сборки | авто |
| Новая сборка перекрывает адрес и токен прошлой установки | авто |
| Выбор озвучки и модели переживает переустановку | авто |
| Хвостовой слэш в адресе срезается | авто |
| Сломанный storage не оставляет экран пустым | авто |
| Сработавший способ доставки записи запоминается | авто |
| Заметка без сети откладывается вместе со звуком; cache-запись переносится в `internal://files` | авто |
| Очередь переживает перезапуск приложения | авто |
| Отказ storage не выдаётся за успешное сохранение | авто |
| Протухшая запись (старше недели) не всплывает | авто |
| Заметки из прошлой версии приложения не теряются | авто |
| Успешная досылка убирает заметку, неудачная сохраняет | авто |
| Пропавший файл записи выбрасывается и не блокирует очередь | авто |
| Две одновременные досылки не создают дубль | авто |
| Очередь не растёт бесконечно (50 записей) | авто |
| Запрос уходит с токеном и ключом идемпотентности | авто |
| Надиктованная заметка идёт с `preview=1`, отложенная — без | авто |
| Вопрос голосом идёт без `intent` | авто |
| Пустая запись не уходит в сеть | авто |
| Синхронный отказ записи/загрузки озвучки не оставляет экран занятым | авто |
| Остановка и окончание озвучки завершают состояние воспроизведения | авто |
| Повторная отправка офлайн-записи сохраняет исходный ключ идемпотентности | авто |
| После успешной досылки постоянная копия аудио удаляется | авто |
| Ошибки шлюза объясняются человеческим языком (401, 429, 502…) | авто |
| Мусор вместо JSON не роняет приложение | авто |
| Молчащий системный модуль не вешает приложение навсегда | авто |
| Тип сети различает Wi-Fi, телефон и отсутствие сети | авто |

## Исторически подтверждено на живых часах

Эти результаты получены в предыдущих прогонах и не являются повторной
приёмкой текущей сборки с `versionCode 53`.

| Сценарий | Когда и чем подтверждено |
| --- | --- |
| Установка и запуск на Xiaomi Watch S5 | фото экрана |
| Сеть через телефон без Wi-Fi | отчёт диагностики: `сеть: bluetooth`, /health 200 |
| Ответ AI на голосовой вопрос | лог шлюза: `POST /api/v1/voice 200 1538 мс` |
| Заметки, список, удаление | сквозная проверка по живому адресу |
| Запись доезжает до провайдера | после перехода на base64: формат распознан как audio/ogg |
| Полный длинный AI-ответ звучит без обрыва | физический Xiaomi Watch S5, v89: две WAV-части проиграны до конца |

Озвучка на самих часах подтверждена для v89; устойчивый транспорт и границы
изменений описаны в [audio-baseline.md](audio-baseline.md).

## Часы: только на устройстве

Это то, что нельзя проверить ни тестом, ни эмулятором.

| Сценарий | Почему только на часах |
| --- | --- |
| Запись голоса `@system.record` | в эмуляторе нет микрофона, запись выходит нулевой |
| Какой способ доставки записи сработал (`bytes` / `upload`) | зависит от прошивки |
| Есть ли `@system.uploadtask` | модуль ни разу не проверялся зондом |
| Раскладка на круглом экране 480×480 | геометрия устройства |
| Системные диалоги подтверждения (заметка, дом, удаление) | мелкие цели не попадают в сетку касаний эмулятора |
| Озвучка ответа после изменения аудиосхемы | нужен настроенный TTS, динамик и новый физический A/B-прогон; текущий v89 уже принят |
| **Сеть через телефон при выключенном Wi-Fi** | решает архитектуру, см. `architecture.md` |
| Работа офлайн-очереди целиком | нужна настоящая потеря связи |

## Как гонять

```bash
npm test              # всё: шлюз и логика часов
npm run test:server   # только шлюз
npm run test:watch    # только часы
npm run smoke         # живой шлюз, который уже запущен
```

Тесты часов работают на обычном Node: системные модули Vela подменяются
заглушками из `watch/testkit/`, которые воспроизводят повадки настоящего
рантайма — колбэки вместо промисов, ошибки с кодом вторым аргументом и
возможность вообще не ответить. Последнее важнее прочего: приложение уже
зависало навсегда из-за молчащего модуля, и теперь на это есть тест.
